pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}

dependencyResolutionManagement {
    repositories {
        mavenLocal()
        mavenCentral()
        google()
    }
}

include ':kydra-log'
project(':kydra-log').projectDir = new File(settingsDir, './library/')

// Common
include ':example'
include ':example:common'
include ':example:android'
include ':example:js'